# Ejercicios (ALUMNO) — Promesas y async/await con APIs

Abre cada carpeta `ejercicio-0X` y ejecuta `index.html` con **Go Live**.

- Trabaja con `let` y `const`.
- Mira la consola para depurar.
- Dentro de cada `app.js` hay pistas y huecos para completar.

API usada en los ejercicios: JSONPlaceholder (pública).
